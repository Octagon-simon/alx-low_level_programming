#include <stdio.h>
#include "main.h"
/**
* _abs - Computes absolute value of an integer
*
* @r: The integer to check
*
* Return: Always 0
*
*/
int _abs(int r)
{
int abs(int);
int x;
x = abs(r);
return (x);
}
